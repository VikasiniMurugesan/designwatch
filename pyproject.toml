[project]
name = "designwatch"
version = "0.1.0"
description = "AI-powered UI/UX design audit agent for automated visual regression testing"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.2",
]
